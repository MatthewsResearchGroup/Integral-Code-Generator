/* This code is designed to generate the recursive code for def coefficients
 * in MD algorithms ()
 */


void def_rr(int N, int n, int np)
{
    if (N == 0 and n == 0 and np == 0)
        return 1.0;
    else if (N > n or N > np)
        return 0.0;
    else if (N > n )

}
